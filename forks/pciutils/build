#!/bin/sh -e

make \
    CC="${CC:-cc}" \
    ZLIB=yes

make \
    CC="${CC:-cc}" \
    SHARED=no \
    OPT="$CFLAGS -fPIC" \
    ZLIB=yes \
    PREFIX=/usr \
    SBINDIR=/usr/bin \
    SHAREDIR=/usr/share/hwdata \
    DESTDIR="$1" install install-lib
